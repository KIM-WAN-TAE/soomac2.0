# motor\_connect.cpp 개선 제안서

본 문서는 `gravity_dh_param.json`을 기준으로, 현재 `motor_connect.cpp`의 중력보상 실패(FF≈0) 및 `d1` 덮어쓰기 문제를 해소하고, 정상상태 오차(특히 J2/J3의 ±150 cnt)를 줄이기 위한 구조적 개선안을 상세히 정리한 것입니다.

-----

## 0\. 배경 및 목표

### 증상

  - J2/J3 축에서 정상상태 오차가 잔존(±150 cnt 정도).

### 핵심 원인(요약)

  - **중력벡터 파싱 실패**: 중력보상(FF)이 사실상 0으로 주입됨.
  - **`d1` 덮어쓰기**: 조인트 2\~4의 `"d": 0.0`에 의해 유효한 `d1`이 0으로 바뀜.
  - **기타**: 영점(`zero_count_`)/부호(`sign_`) 미정렬, Anti-windup 부족, FF가 단일 클램프에 묻힘.

### 목표

  - FF가 정확히 계산·주입되도록 **정식 JSON 파싱**으로 전면 교체.
  - 영점/부호/게인 **파라미터화**로 현장 캘리브레이션 용이성 확보.
  - **Anti-windup + FF 헤드룸**으로 포화 구간에서도 정상상태 오차 축소.

-----

## 1\. 오류 원인과 해결 요약

### 1.1 오류 1: 중력벡터 파싱 실패 → FF≈0

  - **기존 구현**: 라인 문자열 탐색 기반(한 줄에서 `"gravity"`와 `"x"`/`"y"`/`"z"` 동시 존재 가정).
  - **문제점**: 실제 JSON은 중첩 오브젝트이며 키가 서로 다른 줄에 있어 파싱 실패 가능성이 높음. 결과적으로 `gvec_=(0,0,0)` 상태로 남아 중력보상이 0으로 작동.
  - **해결**: `nlohmann::json` 등 정식 JSON 파서 사용. `j["gravity_dh_parameters"]["gravity_vector"]["z"]`처럼 키 경로를 명시적으로 접근.

### 1.2 오류 2: d1 덮어쓰기

  - **기존 구현**: `"d"` 키를 발견할 때마다 `params.d1`에 대입.
  - **문제점**: 조인트 2\~4의 `"d": 0.0`이 뒤에서 등장하면서 유효한 `d1`이 0으로 덮여씌워짐.
  - **해결**: 정식 파서로 `joint_id==1`의 `dh_params["d"]`를 명시적으로 읽어오기.
      - 예: `j["gravity_dh_parameters"]["joints"][0]["dh_params"]["d"]`

-----

## 2\. 개선 사항 목록

1.  **JSON 파싱 전면 교체**: `nlohmann::json` 도입, 구조적 키 경로 접근으로 파싱 신뢰성 확보.
2.  **구조체 안전 초기화**: `DHParameters`, `LinkInertialData` 및 `Vector3d`를 0으로 명시 초기화.
3.  **경로 처리 (ROS 표준 + 폴백)**: `ament_index_cpp::get_package_share_directory` 사용, 실패 시 기존 하드코딩 경로 폴백.
4.  **영점/부호/게인 파라미터화**: `zero_count_`, `sign_`, PID 게인, `K_GFF` 등 ROS 파라미터로 노출.
5.  **Anti-windup 적용**: 최소 구현은 “포화 시 적분 정지(조건부 적분)”, 가능하면 “백계산(Back-calculation)”.
6.  **FF 헤드룸 확보**: 최종 클램프 전에 FF에 우선 헤드룸을 배정해 FF가 잘리지 않도록 함.
7.  **로깅 강화**: 노드 시작 시 파싱 값 전량 로그, 초기 5초 고주기(예: 20Hz)로 FF/포화율 점검.
8.  **안전성·견고성**: NaN 가드, 범위 클램프 정합성, 예외 처리 명료화.
9.  **물리 상수·단위 주석 정비**: `A_per_count_`, `Kt_`, `N_`, `eta_` 등 단위 일관성 명시.
10. **검증 절차 정립**: 파싱 검증 → FF만 주입 테스트 → 영점/부호 캘리브레이션 → KI/Anti-windup 조정 → 다양한 포즈/하중에서 재확인.

-----

## 3\. 구현 가이드

### 3.1 CMake 의존성 추가

단일 헤더(`json.hpp`)를 vendor로 포함해도 무방합니다.

```cmake
# CMakeLists.txt
find_package(nlohmann_json 3.2.0 REQUIRED)
target_link_libraries(${PROJECT_NAME} nlohmann_json::nlohmann_json)
```

### 3.2 구성 파일 경로 획득 (ROS 표준 + 폴백)

```cpp
#include "ament_index_cpp/get_package_share_directory.hpp"

std::string config_path;
try {
  config_path = ament_index_cpp::get_package_share_directory("dongsoo_description") + "/config";
} catch (...) {
  // 개발 환경 폴백
  config_path = "/home/pc/soomac_ws/src/dongsoo_description/config";
}
```

### 3.3 자료구조 안전 초기화

```cpp
struct DHParameters {
  double d1 = 0.0, a2 = 0.0, a3 = 0.0, a4 = 0.0;
  double alpha1 = 0.0, theta2_offset = 0.0;
  Eigen::Vector3d gravity_vector = Eigen::Vector3d::Zero();
};

struct LinkInertialData {
  double mass = 0.0;
  Eigen::Vector3d com = Eigen::Vector3d::Zero();
};
```

### 3.4 DH 파라미터 파서 (정식)

```cpp
#include <nlohmann/json.hpp>
using json = nlohmann::json;

DHParameters loadDHParameters(const std::string& config_path) {
  std::ifstream ifs(config_path + "/gravity_dh_param.json");
  if (!ifs.is_open()) throw std::runtime_error("Cannot open gravity_dh_param.json");
  json j; ifs >> j;

  const auto& g = j.at("gravity_dh_parameters");
  const auto& joints = g.at("joints");

  DHParameters p{};
  // joint_id==1 (index 0)
  p.d1     = joints.at(0).at("dh_params").at("d").get<double>();
  p.alpha1 = joints.at(0).at("dh_params").at("alpha").get<double>();

  // joint_id==2,3,4 (index 1,2,3)
  p.a2 = joints.at(1).at("dh_params").at("a").get<double>();
  p.a3 = joints.at(2).at("dh_params").at("a").get<double>();
  p.a4 = joints.at(3).at("dh_params").at("a").get<double>();

  // theta2_offset(optional)
  if (joints.at(1).at("dh_params").contains("theta_offset")) {
    p.theta2_offset = joints.at(1).at("dh_params").at("theta_offset").get<double>();
  }

  // gravity_vector
  const auto& gv = g.at("gravity_vector");
  p.gravity_vector = Eigen::Vector3d(
    gv.at("x").get<double>(),
    gv.at("y").get<double>(),
    gv.at("z").get<double>()
  );
  return p;
}
```

### 3.5 링크 관성 파서 (정식)

```cpp
std::vector<LinkInertialData> loadLinkInertialData(const std::string& config_path) {
  std::ifstream ifs(config_path + "/link_inertial.json");
  if (!ifs.is_open()) throw std::runtime_error("Cannot open link_inertial.json");
  json j; ifs >> j;

  std::vector<LinkInertialData> links;
  for (const auto& e : j.at("links")) {
    LinkInertialData L{};
    L.mass = e.at("mass").get<double>();
    const auto& c = e.at("center_of_mass");
    L.com = Eigen::Vector3d(c.at("x").get<double>(), c.at("y").get<double>(), c.at("z").get<double>());
    links.push_back(L);
  }
  return links;
}
```

### 3.6 영점/부호/게인 파라미터화

```cpp
// 생성자에서 선언
declare_parameter<std::vector<int32_t>>("zero_count", {2048,2048,2048,2048});
declare_parameter<std::vector<int>>    ("sign",       {+1,+1,+1,+1});
declare_parameter<std::vector<double>> ("kp", {0.5, 1.25, 1.0, 1.0});
declare_parameter<std::vector<double>> ("ki", {0.0, 0.02, 0.02, 0.0}); // FF 정상화 후 소폭 상향
declare_parameter<std::vector<double>> ("kd", {0.012, 0.015, 0.012, 0.01});
declare_parameter<std::vector<double>> ("k_gff", {1.0, 1.0, 1.0, 1.0});

// 적용
auto zc = get_parameter("zero_count").as_integer_array();
for (int i=0;i<4;i++) zero_count_[i] = static_cast<int32_t>(zc[i]);

auto sg = get_parameter("sign").as_integer_array();
for (int i=0;i<4;i++) sign_[i] = static_t<int>(sg[i]);

auto kp = get_parameter("kp").as_double_array();
auto ki = get_parameter("ki").as_double_array();
auto kd = get_parameter("kd").as_double_array();
for (int i=0;i<4;i++) {
  // 멤버 게인(KP_POS_GAINS 등)이 const라면 non-const로 전환하거나 별도 저장소로 반영
}

auto kff = get_parameter("k_gff").as_double_array();
for (int i=0;i<4;i++) {
  // K_GFF 반영
}
```

**파라미터 YAML 예시**

```yaml
# dxl_current_node_params.yaml
dxl_current_node:
  ros__parameters:
    zero_count: [2048, 2048, 2048, 2048]
    sign: [1, 1, 1, 1]
    kp: [0.5, 1.25, 1.0, 1.0]
    ki: [0.0, 0.02, 0.02, 0.0]
    kd: [0.012, 0.015, 0.012, 0.01]
    k_gff: [1.0, 1.0, 1.0, 1.0]
```

### 3.7 Anti-windup + FF 헤드룸

```cpp
// 중력보상 전류(raw)
float u_ff = static_cast<float>(tau_g_raw[i]);

// PID 전류(raw)
float u_pid = KP_POS_GAINS[i]*pos_err + KI_POS_GAINS[i]*integral_error_[i] + KD_POS_GAINS[i]*dpos;

// 소프트 리밋
float limit = (dxl_ids_[i]==4) ? 500.0f : 900.0f;

// FF 우선 헤드룸 배정
float pid_limit = std::max(0.0f, limit - std::abs(u_ff));
u_pid = std::clamp(u_pid, -pid_limit, +pid_limit);

// 합산
float u = u_ff + u_pid;

// 포화 판정
bool saturated = (std::abs(u) >= limit - 1e-6f);

// 조건부 적분(포화 아닐 때만 적분 누적)
if (!saturated) {
  integral_error_[i] = std::clamp(integral_error_[i] + pos_err*dt,
                                  -MAX_INTEGRAL_ERROR, +MAX_INTEGRAL_ERROR);
}

// 최종 클램프
u = std::clamp(u, -limit, +limit);

// SyncWrite
int32_t goal_current = static_cast<int32_t>(std::lround(u));
```

> **확장**: 백계산 방식(anti-windup back-calculation)을 사용하면 포화 시 `integral_error`를 `(u - u_unclamped)/β`로 보정하여 더 빠르게 정상으로 복귀시킬 수 있습니다.

### 3.8 로깅 강화

  - **노드 시작 직후 1회**:
      - 파싱된 `d1`/`a2`/`a3`/`a4`/`alpha1`/`theta2_offset`, `gravity_vector`, 각 링크 `m`, `com`을 `RCLCPP_INFO`로 출력.
  - **초기 5초간 (예: 20Hz)**:
      - `q(rad)`, `tau_g(Nm)`, `tau_g_raw(LSB)`, `|u|/limit(%)` 포화율을 출력해 FF가 0이 아님을 눈으로 확인.
  - **평상시**: 5초 주기 로그 유지(현 코드의 디버그 로그에 포화율 추가 권장).

### 3.9 안전성·견고성 보강

  - 연산 전 `std::isfinite`로 NaN/inf 가드.
  - `goal_current`는 16-bit signed 범위 내로 안전하게 유지(소프트/하드리밋과 정합).
  - SDK 응답은 `COMM_SUCCESS` 외에도 필요 시 에러 플래그를 분리해 로깅(가능한 API에서).
  - 예외 처리 시 노드 종료 순서 명확화(현 코드 양호).

### 3.10 물리 상수·단위 명시

```
A_per_count_ = 0.00269 [A/LSB]
I[A] = τ_out[Nm] / (Kt[Nm/A] * N * η)
raw[LSB] = I / A_per_count
COUNT2RAD_ = 2π/4096
```

> 주석으로 단위를 명확히 유지하면 후속 유지보수 시 오류를 줄일 수 있습니다.

-----

## 4\. 검증 절차

1.  **파싱 검증**
      - 시작 로그에서 `gravity_vector.z = -9.81`, `d1 = 0.11575`, `a2=0.25`, `a3=0.25`, `a4=0.2159`, `alpha1=π/2`, `theta2_offset=π/2`가 올바르게 출력되는지 확인.
2.  **FF만 주입 테스트**
      - PID 게인을 임시로 낮추거나 0에 가깝게 해보고, 임의의 포즈에서 팔이 중력으로 처지지 않고 어느 정도 버티는지 확인(FF가 살아있음 확인).
3.  **영점/부호 캘리브레이션**
      - 기계 기준 포즈에서 `zero_count_`를 실제 엔코더 값과 일치시키고, 원하는 `+전류`/`회전 방향`에 맞춰 `sign_`을 조정.
      - 목표 포즈에서 정상상태 오차 감소 확인.
4.  **Anti-windup + KI 조정**
      - J2/J3의 KI를 소폭 상향(예: 0.01 → 0.02)하고, Anti-windup 적용 전후 정상상태 오차 및 수렴 속도 비교.
5.  **다양한 포즈/하중 시험**
      - 서로 다른 관절 각도, 말단 하중 변화에서 포화율과 정상상태 오차를 기록·비교.

-----

## 5\. 체크리스트

  - [ ] `nlohmann::json` 도입 및 빌드 성공
  - [ ] `loadDHParameters`/`loadLinkInertialData`를 정식 파싱으로 교체
  - [ ] 시작 로그에 파싱 결과 전량 출력
  - [ ] `zero_count_`/`sign_`/PID 게인/`K_GFF` 파라미터화
  - [ ] Anti-windup(조건부 적분 또는 백계산) 적용
  - [ ] FF 헤드룸 확보 로직 적용
  - [ ] 초기 5초 고주기 진단 로그로 FF/포화율 확인
  - [ ] 정상상태 오차(특히 J2/J3) 개선 확인

-----

## 6\. 자주 하는 실수와 방지 팁

  - **실수**: 중첩 JSON을 라인 탐색으로 파싱하여 핵심 값(중력벡터)이 0으로 남는 문제
      - **→ 방지**: 반드시 정식 파서로 전환.
  - **실수**: `d1`을 첫 등장 `"d"`로만 판단해 뒤에서 0으로 덮는 문제
      - **→ 방지**: joint index/키 경로로 명시 접근.
  - **실수**: FF와 PID를 한 번에 클램프하여 FF가 소실되는 문제
      - **→ 방지**: FF 우선 헤드룸 확보 후 PID를 클램프.
  - **실수**: 포화 상태에서도 적분이 계속 누적되어 흔들리거나 느리게 복귀
      - **→ 방지**: 조건부 적분 또는 백계산 anti-windup 적용.
  - **실수**: 영점/부호를 코드에 하드코딩
      - **→ 방지**: ROS 파라미터화로 빠른 현장 캘리브레이션.

-----

## 7\. 결론

본 개선안의 핵심은 **정식 JSON 파싱**과 \*\*제어기 현실화(영점/부호/anti-windup/FF 헤드룸)\*\*입니다. 먼저 파싱 경로를 정비해 FF가 확실히 살아있게 만든 다음, 영점 및 게인을 현장 파라미터로 조정하고 anti-windup을 적용하면, 보고된 정상상태 오차(±150 cnt)는 실전적으로 의미 있게 줄어들 것입니다.